ataque do tipo XSS (cross site scripting) 

o invasor pode inserir scripts no nosso sistema

para não inserir o mysqli real escape string e o htmlspecialchars podemos usar funções para isso

ex function clear($input){
	global $connect
	$var $name = mysqli_real_escape_string($connect, 	$input);

	$var = htmlespecialchars($var);

assim estaremos usando dois filtros